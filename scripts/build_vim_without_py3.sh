#!/bin/sh

./configure \
    --with-features=huge \
    --with-compiledby="wsdjeg" \
    --enable-multibyte \
    --enable-gpm \
    --prefix=/usr \
    --enable-cscope \
    --enable-fontset \
    --enable-sniff \
    --enable-xim \
    --enable-fail-if-missing \
    --enable-mzschemeinterp \
    --enable-pythoninterp \
